/*
class Prompt {
  constructor(fn, type, name, message, choices) {
    this.action = fn;
    this.config = {
      type,
      name,
      message,
      choices
    }
  }
}



const hello = () => {
  console.log('Hello');
};

const config = {
  type: 'select',
      name: 'next',
      message: 'Меню',
      choices: [
        { title: 'Новая игра', value: 'startGame' },
        { title: 'Продолжить', value: 'saves' },
        { title: 'Сохранить', value: 'save' },
        { title: 'Выйти', value: 'endGame' },
      ],
}
const menu = new Prompt(hello, 'select', 'Menu', choices);
console.log(menu);

export default Prompt;
*/